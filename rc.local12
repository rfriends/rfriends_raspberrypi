#
# Print the IP address
_IP=$(hostname -I) || true
if [ "$_IP" ]; then
  printf "My IP address is %s\n" "$_IP"
fi
# -----------------------------------------
# rfriends
# -----------------------------------------
# wifi power management off
nmcli con mod preconfigured wifi.powersave disable
# -----------------------------------------
# lighttpd
rm /home/rfriendsuser/lighttpd/error.log
# -----------------------------------------
_IP=$(hostname -I) || true
if [ "$_IP" ]; then
  sudo sh -c "echo $_IP > /boot/firmware/IPaddress.txt"
  sudo sh -c "date >> /boot/firmware/IPaddress.txt"
fi

sh /home/rfriendsuser/rfriends3/rfriends3_boot.sh
